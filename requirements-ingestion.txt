# -------------------------------------------------------
# INGESTION PIPELINE — local use only, not deployed
# Install with: pip install -r requirements-ingestion.txt
# -------------------------------------------------------

# Includes everything the API server needs
-r requirements.txt

# PDF parsing
pymupdf>=1.24.0
pytesseract>=0.3.10
pdf2image>=1.16.0
unstructured[all-docs]>=0.12.0

# ML extras (local model loading, chunking)
transformers>=4.38.0
torch>=2.2.0
accelerate>=0.27.0
rank-bm25>=0.2.2

# Progress & utilities
tqdm>=4.66.0
